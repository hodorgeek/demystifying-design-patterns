package example1.simuduck.quack;

/**
 * Like flyBehavior the same thing here for the quack behavior; We have an interface
 * that just includes a quack() method that needs to be implemented.
 */
public interface QuackBehavior {
    void quack();
}
